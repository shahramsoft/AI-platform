# rag

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rag` to build the library.

## Running unit tests

Run `nx test rag` to execute the unit tests via [Vitest](https://vitest.dev/).
